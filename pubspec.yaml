name: lsileveleditor
description: A program to edit and create game levels.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 0.0.2

environment:
  sdk: ">=2.17.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  file_picker: 5.0.1
  flutter_colorpicker: ^1.0.3
  cupertino_icons: ^1.0.2
  ember:
    git: 
      url: https://github.com/Knightro63/ember.git
  css:
    git:
      url: https://github.com/Knightro63/css.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3

flutter:
  uses-material-design: true
